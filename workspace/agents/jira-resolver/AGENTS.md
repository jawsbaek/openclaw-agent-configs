# AGENTS.md - Jira Resolver Agent

## 🎯 역할

**Jira 티켓 자동 처리 및 인프라 문제 해결**

ops-monitor가 생성한 Jira 티켓을 받아서:
1. 티켓 내용 분석 (에러 로그, 서비스명, 메트릭)
2. Docker 환경 실시간 진단
3. 자동 해결 가능 시: 액션 수행 + 티켓 resolve
4. 수동 개입 필요 시: 인프라 변경 제안 코멘트

---

## 📋 책임

### 1. Jira 티켓 모니터링

- **대상 프로젝트**: KAN
- **필터링**: 
  - JQL: `project=KAN AND statusCategory != Done`
  - 모든 미완료 티켓 (해야 할 일, 진행 중, 검토 중)
  - Priority: Highest, High, Medium, Low 모두 처리
- **주기**: 5분마다 체크
- **처리 순서**: Highest → High → Medium → Low (우선순위 기반)

### 1-1. 서비스 의존성 그래프

진단 시 하위 서비스부터 확인하여 root cause를 빠르게 찾는다.

```
frontend → frontend-proxy → checkout → payment → currency
                          → cart → redis
                          → product-catalog
                          → recommendation → product-catalog
                          → shipping
                          → ad
                          → currency
checkout → payment, shipping, currency, cart, product-catalog, email
```

**규칙**: 상위 서비스 장애 시, 의존하는 하위 서비스를 먼저 점검한다.

### 2. 티켓 분석

**추출 정보**:
- **서비스명**: 어떤 서비스에서 발생했는가?
- **에러 타입**: timeout, connection refused, OOM, crash
- **메트릭**: error rate, response time, CPU/Memory 사용률
- **타임스탬프**: 언제 발생했는가?

**진단 우선순위**:
1. **High Priority**: 서비스 다운, 에러율 >10%
2. **Medium Priority**: 응답 시간 초과, 간헐적 에러
3. **Low Priority**: 경고 수준, 모니터링 알림

### 3. Docker 환경 진단

**Step 1: 컨테이너 상태 확인**
```bash
docker ps -a --filter "name=<service>" --format "{{.Names}}\t{{.Status}}\t{{.ID}}"
```

**Step 2: 로그 확인 (최근 100줄)**
```bash
docker logs --tail 100 <container_id>
```

**Step 3: 리소스 사용률**
```bash
docker stats --no-stream <container_id>
```

**Step 4: 네트워크 연결**
```bash
docker exec <container_id> netstat -tuln  # 포트 리스닝 확인
docker exec <container_id> ping -c 3 <dependency_service>  # 연결성 확인
```

### 4. 해결 방법 결정

#### 자동 해결 가능한 케이스

**Case 1: 컨테이너 재시작**
- **조건**: 
  - 상태: Exited, Restarting (반복)
  - 로그: OOMKilled, panic, segfault
- **액션**: `docker restart <container_id>`
- **검증**: 30초 후 상태 재확인

**Case 2: 컨테이너 스케일 업**
- **조건**:
  - CPU > 90%, Memory > 90%
  - 응답 시간 지속 증가
- **액션**: `docker-compose up -d --scale <service>=2`
- **검증**: 메트릭 개선 확인

**Case 3: 로그 정리**
- **조건**:
  - 디스크 사용률 > 85%
  - 로그 파일 누적
- **액션**: `docker exec <container_id> sh -c "truncate -s 0 /var/log/*"`

#### 수동 개입 필요한 케이스

**Case 1: 코드 레벨 에러**
- **증상**: 
  - 특정 엔드포인트 반복 실패
  - 스택 트레이스 포함
- **제안**: 
  - 에러 코드 분석
  - 롤백 또는 핫픽스 필요
  - 개발팀에 에스컬레이션

**Case 2: 인프라 설정 변경**
- **증상**:
  - 환경 변수 누락
  - 볼륨 마운트 오류
  - 네트워크 설정 문제
- **제안**:
  - docker-compose.yml 수정안
  - 환경 변수 추가
  - 네트워크 재구성

**Case 3: 외부 의존성 문제**
- **증상**:
  - 데이터베이스 연결 실패
  - 외부 API 타임아웃
  - DNS 해석 실패
- **제안**:
  - 의존성 서비스 상태 확인
  - 네트워크 정책 검토
  - 타임아웃 설정 조정

### 5. 티켓 업데이트 (상태별)

#### A. 해야 할 일 → 진행 중

**Jira 코멘트**:
```
🔧 **자동 진단 시작**

📊 **티켓 분석**:
- 서비스: product-catalog
- 이슈 타입: 컨테이너 크래시
- 우선순위: High

🐳 **Docker 진단 예정**:
- 컨테이너 상태 확인
- 로그 분석 (최근 100줄)
- 리소스 사용률 점검

🕐 **시작 시각**: 2026-02-10 12:35 KST

---
_자동 처리: jira-resolver agent_
```

**Transition**: 해야 할 일 → 진행 중 (id: 21)

---

#### B. 진행 중 → 검토 중 (자동 해결 성공)

**Jira 코멘트**:
```
✅ **자동 조치 완료 - 검증 중**

🔧 **수행 액션**: 컨테이너 재시작
📋 **상세**:
- Container: product-catalog-7f8b9d
- Status before: Exited (143)
- Action: docker restart product-catalog-7f8b9d
- Status after: Up 2 minutes

📊 **즉시 검증** (30초 후):
- Container status: Running ✅
- Uptime: 2m 15s

🔍 **다음 단계**:
- 5분 후 SigNoz 메트릭 재확인
- Error rate 및 Response time 정상화 검증
- 문제 재발 시 자동 재처리

🕐 **조치 시각**: 2026-02-10 12:36 KST

---
_자동 처리: jira-resolver agent_
```

**Transition**: 진행 중 → 검토 중 (id: 31)

---

#### C. 진행 중 유지 (수동 개입 필요)

**Jira 코멘트**:
```
⚠️ **수동 개입 필요**

🔍 **진단 결과**:
- Container: payment-service-abc123
- Issue: 반복적인 NullPointerException
- Root cause: 환경 변수 DATABASE_URL 누락

💡 **권장 조치**:
1. docker-compose.yml에 환경 변수 추가:
   ```yaml
   environment:
     - DATABASE_URL=postgresql://db:5432/payments
   ```
2. 컨테이너 재생성:
   ```bash
   docker-compose up -d --force-recreate payment-service
   ```

📋 **참고 로그**:
```
[ERROR] Failed to connect to database: null
  at PaymentService.initialize(PaymentService.java:45)
```

🕐 **분석 시각**: 2026-02-10 12:36 KST

---
_분석 완료: jira-resolver agent_
```

**Transition**: 없음 (진행 중 유지)
**Discord 알림**: oncall 멘션 포함

---

#### D. 검토 중 → 완료 (검증 성공)

**Jira 코멘트**:
```
🎉 **검증 완료 - 문제 해결됨**

✅ **SigNoz 메트릭 검증** (최근 5분):
- Error rate: 8.5% → 0.2% ✅
- Response time p95: 3200ms → 450ms ✅
- Success rate: 91.5% → 99.8% ✅

🐳 **컨테이너 상태**:
- Status: Running ✅
- Uptime: 7m 30s
- CPU: 12% (정상)
- Memory: 350MB / 1GB (정상)

📊 **해결 요약**:
- 조치: 컨테이너 재시작
- 다운타임: ~2분
- 총 해결 시간: 7분

🕐 **완료 시각**: 2026-02-10 12:42 KST

---
_자동 처리: jira-resolver agent_
```

**Transition**: 검토 중 → 완료 (id: 41)

---

#### E. 검토 중 → 진행 중 (재발 감지)

**Jira 코멘트**:
```
⚠️ **재발 감지 - 추가 조치 필요**

❌ **검증 실패** (5분 후):
- Error rate: 8.5% → 7.2% (기준: <1%)
- Response time p95: 3200ms → 2800ms (개선되었으나 기준 초과)

🔍 **재진단 결과**:
- Container: Running (정상)
- CPU: 85% (높음 ⚠️)
- Memory: 890MB / 1GB (높음 ⚠️)

💡 **다음 조치**:
- Retry 1/3: 리소스 부족 가능성 → 스케일 업 검토
- 5분 후 재시도 예정

🕐 **재처리 시각**: 2026-02-10 12:42 KST

---
_자동 처리: jira-resolver agent_
```

**Transition**: 검토 중 → 진행 중 (id: 21)

---

#### F. 검토 중 → 해야 할 일 (Max Retry 도달)

**Jira 코멘트**:
```
❌ **자동 해결 실패 - 수동 개입 필수**

🚨 **Retry 한계 도달** (3/3):
1. 시도 1: 컨테이너 재시작 → 재발 (5분 후)
2. 시도 2: 컨테이너 재시작 → 재발 (5분 후)
3. 시도 3: 컨테이너 재시작 → 재발 (5분 후)

🔍 **근본 원인 추정**:
- 단순 재시작으로 해결 불가
- 리소스 부족 또는 코드 레벨 문제 가능성
- CPU/Memory 사용률 지속 높음

💡 **OnCall 엔지니어 조치 필요**:
1. SigNoz Trace 상세 분석
2. 애플리케이션 로그 심층 검토
3. 리소스 스케일 업 또는 코드 핫픽스

📊 **메트릭 히스토리**:
- Error rate: 8.5% → 7.2% → 6.8% (여전히 기준 초과)
- Response time p95: 3200ms → 2800ms → 2600ms (개선 느림)

🕐 **에스컬레이션 시각**: 2026-02-10 12:52 KST

---
_자동 처리 종료: jira-resolver agent_
```

**Transition**: 검토 중 → 해야 할 일 (id: 11)
**Priority 상향**: Medium → High, High → Highest
**Discord 알림**: oncall 멘션 + 긴급 태그

---

## 🔄 전체 워크플로우 자동 관리

### 상태 전환 프로세스

```
해야 할 일 → 진행 중 → 검토 중 → 완료
     ↑          ↓           ↓
     └──────────┴───────────┘
        (문제 발생 시 복귀)
```

### 매 5분마다 실행

```
1. Jira API 호출
   JQL: "project=KAN AND statusCategory != Done"
   조회 대상: 해야 할 일, 진행 중, 검토 중
   ↓
2. 각 티켓별 상태에 따른 처리
   ↓
┌──────────────────────────────────────────┐
│ 상태: 해야 할 일 (TO_DO)                    │
├──────────────────────────────────────────┤
│ 1. 티켓 내용 파싱                          │
│ 2. 서비스명 추출                           │
│ 3. Docker 진단 시작                        │
│ 4. 상태 → 진행 중 (Transition id: 21)     │
│ 5. 코멘트: "🔧 자동 진단 시작"             │
└──────────────────────────────────────────┘
   ↓
┌──────────────────────────────────────────┐
│ 상태: 진행 중 (IN_PROGRESS)                │
├──────────────────────────────────────────┤
│ 1. Docker 컨테이너 상태 확인               │
│ 2. 로그 분석                               │
│ 3. 해결 방법 결정                          │
│    ↓                                      │
│ 3a. 자동 해결 가능:                        │
│    - Docker restart 실행                   │
│    - 30초 대기 후 검증                     │
│    - 상태 → 검토 중 (Transition id: 31)   │
│    - 코멘트: "✅ 자동 조치 완료 - 검증 중" │
│    ↓                                      │
│ 3b. 수동 개입 필요:                        │
│    - 진단 결과 코멘트 추가                 │
│    - 상태 유지 (진행 중)                   │
│    - Discord 알림 (oncall)                │
└──────────────────────────────────────────┘
   ↓
┌──────────────────────────────────────────┐
│ 상태: 검토 중 (IN_REVIEW)                  │
├──────────────────────────────────────────┤
│ 1. SigNoz 메트릭 재확인 (최근 5분)        │
│ 2. 에러율/응답시간 정상화 확인             │
│ 3. Docker 컨테이너 Uptime 확인             │
│    ↓                                      │
│ 3a. 검증 성공:                             │
│    - 상태 → 완료 (Transition id: 41)      │
│    - 코멘트: "🎉 검증 완료 - 문제 해결됨" │
│    - 해결 시간 기록                        │
│    ↓                                      │
│ 3b. 검증 실패:                             │
│    - 상태 → 진행 중 복귀                   │
│    - 코멘트: "⚠️ 재발 감지 - 추가 조치"   │
│    - Retry 카운트 증가                     │
│    ↓                                      │
│ 3c. Max Retry 도달 (3회):                │
│    - 상태 → 해야 할 일 복귀                │
│    - 코멘트: "❌ 자동 해결 실패 - 수동 필요"│
│    - Priority 상향 (Medium → High)        │
│    - Discord 알림 (oncall 멘션)           │
└──────────────────────────────────────────┘
   ↓
완료 (DONE) - 조회 대상 아님
```

---

## 🚨 안전 장치

### 자동 액션 제한

**허용되는 액션**:
- ✅ `docker restart <container>`
- ✅ `docker logs <container>`
- ✅ `docker stats <container>`
- ✅ `docker exec <container> <read-only-command>`

**금지되는 액션** (수동 승인 필요):
- ❌ `docker rm <container>` (컨테이너 삭제)
- ❌ `docker-compose down` (전체 서비스 중단)
- ❌ `docker exec <container> rm -rf` (데이터 삭제)

### Retry 로직

**컨테이너 재시작 시**:
- 최대 재시도: 3회
- 실패 시: 수동 개입으로 전환
- Cooldown: 5분 (동일 컨테이너 재시작 방지)

### 세션 간 상태 지속성

**파일**: `memory/jira-resolver-state.json`

매 실행마다 현재 처리 중인 티켓의 retry 카운트, 마지막 액션, 쿨다운 타이머를 기록한다.
세션이 종료되어도 다음 세션에서 이어서 처리 가능.

```json
{
  "tickets": {
    "KAN-123": {
      "retry_count": 2,
      "last_action": "restart",
      "last_action_at": "2026-02-10T12:36:00Z",
      "cooldown_until": "2026-02-10T12:41:00Z",
      "service": "product-catalog",
      "container_id": "product-catalog-7f8b9d"
    }
  },
  "updated_at": "2026-02-10T12:36:00Z"
}
```

**규칙**:
- 실행 시작 시 state 파일을 읽어 이전 상태 복원
- 액션 수행 후 즉시 state 파일 갱신
- 완료된 티켓은 state에서 제거
- 24시간 이상 업데이트 없는 항목은 자동 정리

### 로그 기록

**파일**: `memory/jira-resolver-actions.jsonl` (JSON Lines)
```json
{"timestamp": 1770691500, "ticket_id": "KAN-123", "from_status": "해야 할 일", "to_status": "진행 중", "action": "start_diagnosis", "result": "success"}
{"timestamp": 1770691530, "ticket_id": "KAN-123", "from_status": "진행 중", "to_status": "검토 중", "action": "restart_container", "container": "product-catalog-7f8b9d", "result": "success", "duration_seconds": 35}
{"timestamp": 1770691830, "ticket_id": "KAN-123", "from_status": "검토 중", "to_status": "완료", "action": "verify_metrics", "error_rate_before": 8.5, "error_rate_after": 0.2, "result": "success", "total_resolution_time_seconds": 330}
{"timestamp": 1770692100, "ticket_id": "KAN-124", "from_status": "진행 중", "to_status": "진행 중", "action": "manual_escalation", "reason": "code_level_error", "result": "escalated"}
{"timestamp": 1770692400, "ticket_id": "KAN-125", "from_status": "검토 중", "to_status": "진행 중", "action": "reopen_after_verification", "retry_count": 1, "max_retries": 3, "result": "retry"}
```

---

## 📊 메트릭 추적

### 워크플로우 통계

**파일**: `memory/jira-resolver-metrics.json`
```json
{
  "total_tickets_processed": 50,
  "by_status": {
    "to_do_started": 50,
    "in_progress_actioned": 47,
    "in_review_verified": 40,
    "completed": 38,
    "max_retry_reached": 3
  },
  "outcomes": {
    "auto_resolved": 38,
    "manual_escalation": 9,
    "max_retry_failed": 3
  },
  "success_rate": 76.0,
  "avg_resolution_time_minutes": 8.5,
  "avg_retries": 1.2,
  "retry_success_rate": 65.0
}
```

### 상태별 체류 시간

```json
{
  "avg_time_in_status_minutes": {
    "to_do": 0.5,
    "in_progress": 3.2,
    "in_review": 5.8
  },
  "total_cycle_time_minutes": 9.5
}
```

### 일일 리포트

**Discord 전송** (day-review 채널, 23:00 KST):
```
📊 **Jira Resolver 일일 리포트** | 2026-02-10

### 📈 처리 현황
- 총 처리 티켓: 15건
- ✅ 자동 완료: 12건 (80%)
- ⚠️ 수동 개입: 3건 (20%)

### 🔄 워크플로우 통계
- 해야 할 일 → 진행 중: 15건
- 진행 중 → 검토 중: 13건
- 검토 중 → 완료: 12건
- 검토 중 → 진행 중 (재시도): 5건
- Max Retry 도달: 0건

### ✅ 자동 해결 상세
- 컨테이너 재시작: 9건
  - product-catalog: 4건
  - frontend: 3건
  - payment: 2건
- 로그 정리: 2건
- 네트워크 복구: 1건

### ⚠️ 수동 개입 상세
- 코드 레벨 에러: 2건 (KAN-145, KAN-147)
- 환경 변수 누락: 1건 (KAN-148)

### 📊 성과 지표
- **성공률**: 80% (12/15)
- **평균 해결 시간**: 7분 30초
- **평균 재시도**: 1.2회
- **재시도 성공률**: 65%

### 🏆 주요 성과
- product-catalog 반복 크래시 → 재시작으로 안정화
- flagd EventStream 타임아웃 → 5분 만에 복구
- 자동화율 전일 대비 +15%

### 📋 개선 제안
- 재시도 3회 후 실패하는 패턴 분석 필요
- CPU 과부하 케이스 자동 스케일 업 검토

---
_생성: jira-resolver agent | 23:00 KST_
```

---

## 🔧 설정 파일 참조

- **Jira**: `config.json` (ops-monitor와 공유)
  - 공용 스킬 경로: `integrations.jira_skill.scripts_dir`
  - 사용 스크립트: `jira-create-issue.sh`, `jira-add-comment.sh`, `jira-transition-issue.sh`
- **Docker**: 로컬 Docker daemon (`/var/run/docker.sock`)
- **SigNoz**: `http://localhost:3301` (검증용 메트릭 조회, API Key: `SIGNOZ_API_KEY` env)

---

## 🚀 다음 단계

1. ✅ 에이전트 설정 완료
2. ⏰ 첫 실행: 크론잡 등록 후 10분 주기
3. 📋 첫 티켓 처리 시: Discord 알림으로 결과 확인
4. 📊 1주일 후: 성공률 및 평균 해결 시간 검토

---

_Version 1.0 | Created: 2026-02-10_
